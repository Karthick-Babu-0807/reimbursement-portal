<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
  <div class="container">
    <%= link_to "Reimbursement Portal", root_path, class: "navbar-brand fw-bold" %>
    
    <% if logged_in? %>
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav me-auto">
          <% if current_user.admin? %>
            <li class="nav-item">
              <%= link_to "Employees", employees_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "All Bills", bills_path, class: "nav-link" %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "My Bills", bills_path, class: "nav-link" %>
            </li>
          <% end %>
        </ul>
        
        <div class="d-flex align-items-center text-white">
          <span class="me-3">Hi, <%= current_user.name %></span>
          <%= button_to "Logout", logout_path, method: :delete, class: "btn btn-outline-light btn-sm" %>
        </div>
      </div>
    <% end %>
  </div>
</nav>